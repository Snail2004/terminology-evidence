# Stage A sense casebook: test_003

This casebook contains no Vietnamese candidates. Review the English
sense definition and part of speech from supplied evidence only.

## 1. style transfer

- `sense_id`: `d2lce_041add3a2f3aa452408a5384`
- Split: `test`
- Model definition: A computer vision technique that synthesizes an image by applying the visual style of one image to the content of another.
- Model POS: `noun_phrase`

### Primary contexts

- `ctx_deae144867e9bfadbffe6cf7`: # Neural Style Transfer
- `ctx_d719b8db05528e808274fcea`: Then we will explain how to use style transfer techniques to generate images like the cover of this book.
- `ctx_6b687a2d2cfec26c4434d515`: In this section, we will leverage layerwise representations of a CNN to automatically apply the style of one image to another image, i.e., *style transfer* :cite:`Gatys.Ecker.Bethge.2016`.
- `ctx_e603f93a871e9ee36882fbe5`: This synthesized image is the only variable that needs to be updated during the style transfer process, i.e., the model parameters to be updated during training.
- `ctx_e9df20e49655a73442ecc990`: ![Given content and style images, style transfer outputs a synthesized image.](../img/style-transfer.svg) :label:`fig_style_transfer`

### Backup contexts

- `ctx_0c23b9cfe24725bd1e388026`: :numref:`fig_style_transfer_model` illustrates the CNN-based style transfer method with a simplified example.
- `ctx_654e7ba25bac02b376751b46`: Since deep neural networks can effectively represent images in multiple levels, such layerwise representations have been successfully used in various computer vision tasks such as *object detection*, *semantic segmentation*, and *style transfer*.
- `ctx_1f5ec6ced6190e5f15225183`: ![CNN-based style transfer process.

### Contrastive contexts

- `ctxx_910f4d9a65952b578fc14d53`: Synthetic: The museum studied style transfer across generations of painters, without generating any images computationally.

### Definition evidence

- `ctx_6b687a2d2cfec26c4434d515`: In this section, we will leverage layerwise representations of a CNN to automatically apply the style of one image to another image, i.e., *style transfer* :cite:`Gatys.Ecker.Bethge.2016`.
- `ctx_e9df20e49655a73442ecc990`: ![Given content and style images, style transfer outputs a synthesized image.](../img/style-transfer.svg) :label:`fig_style_transfer`
- `ctx_d719b8db05528e808274fcea`: Then we will explain how to use style transfer techniques to generate images like the cover of this book.

### Part-of-speech evidence

- `ctx_654e7ba25bac02b376751b46`: Since deep neural networks can effectively represent images in multiple levels, such layerwise representations have been successfully used in various computer vision tasks such as *object detection*, *semantic segmentation*, and *style transfer*.
- `ctx_d719b8db05528e808274fcea`: Then we will explain how to use style transfer techniques to generate images like the cover of this book.
- `ctx_deae144867e9bfadbffe6cf7`: # Neural Style Transfer

## 2. switch

- `sense_id`: `d2lce_91002293cea2184b43995f47`
- Split: `test`
- Model definition: a device that connects multiple endpoints and forwards communication between them
- Model POS: `noun`

### Primary contexts

- `ctx_04a4e4586094507de76cea7e`: Note that switches are not unique to traditional computer networks.

### Contrastive contexts

- `ctxx_ed06244d7393e7f5493c7f08`: Synthetic: The network switch connects devices, but to switch off the bias means to disable a model setting.

### Definition evidence

- `ctx_04a4e4586094507de76cea7e`: Note that switches are not unique to traditional computer networks.

### Part-of-speech evidence

- `ctx_ad04343cea039cb88b7c242b`: In contrast, the position of a light switch and the brightness in the room are not (they are not perfectly deterministic, though, since we could always have a broken light bulb, power failure, or a broken switch).
- `ctx_04a4e4586094507de76cea7e`: Note that switches are not unique to traditional computer networks.

## 3. train

- `sense_id`: `d2lce_d1ca2fd5568e11a79b1b3366`
- Split: `test`
- Model definition: to fit or optimize a machine learning model using data
- Model POS: `verb`

### Primary contexts

- `ctx_929bfbfdad0e6f80c43c25a6`: Construct an MLP containing a shared parameter layer and train it.
- `ctx_ea5790fe6044cd61bc133385`: In other words, we *train* our model with data.
- `ctx_9282f3ace8d089f60898e962`: In deep learning, we *train* models, updating them successively so that they get better and better as they see more and more data.
- `ctx_d6a61508bc9d44444618bd65`: In 1989, LeCun published the first study to successfully train CNNs via backpropagation.
- `ctx_6b5fc1533573098a302c2aa6`: When we train high-capacity models we run the risk of overfitting.

### Backup contexts

- `ctx_71176d223ed033be9203e626`: Moreover, for many tasks, those difficult-to-optimize models turn out to be so much better that figuring out how to train them ends up being well worth the trouble.
- `ctx_732118a0817f428f0428816a`: Dump the resulting representations into your favorite classifier, likely a linear model or kernel method, to train a classifier.
- `ctx_c59d1952fd176678e6067a09`: Applying deep learning requires simultaneously understanding (i) the motivations for casting a problem in a particular way; (ii) the mathematical form of a given model; (iii) the optimization algorithms for fitting the models to data; (iv) the statistical principles that tell us when we should expect our models to generalize to unseen data and practical methods for certifying that they have, in fact, generalized; and (v) the engineering techniques required to train models efficiently, navigating the pitfalls of numerical computing and getting the most out of available hardware.

### Contrastive contexts

- `ctxx_341e6741173efa8aa1ba0c71`: Synthetic: They train every morning for the marathon before sunrise.

### Definition evidence

- `ctx_ea5790fe6044cd61bc133385`: In other words, we *train* our model with data.
- `ctx_9282f3ace8d089f60898e962`: In deep learning, we *train* models, updating them successively so that they get better and better as they see more and more data.
- `ctx_d6a61508bc9d44444618bd65`: In 1989, LeCun published the first study to successfully train CNNs via backpropagation.

### Part-of-speech evidence

- `ctx_ea5790fe6044cd61bc133385`: In other words, we *train* our model with data.
- `ctx_9282f3ace8d089f60898e962`: In deep learning, we *train* models, updating them successively so that they get better and better as they see more and more data.
- `ctx_71176d223ed033be9203e626`: Moreover, for many tasks, those difficult-to-optimize models turn out to be so much better that figuring out how to train them ends up being well worth the trouble.

## 4. transpose

- `sense_id`: `d2lce_6b1b75f43b72ec9bace62065`
- Split: `test`
- Model definition: To swap the rows and columns of a matrix or corresponding axes of a tensor.
- Model POS: `verb`

### Primary contexts

- `ctx_8b4f9d8a2db0a1027760532e`: When we exchange a matrix's rows and columns, the result is called the *transpose* of the matrix.
- `ctx_ca28cbd449972202e89c8206`: * $f(\cdot)$: a function * $\log(\cdot)$: the natural logarithm (base $e$) * $\log_2(\cdot)$: logarithm with base $2$ * $\exp(\cdot)$: the exponential function * $\mathbf{1}(\cdot)$: the indicator function, evaluates to $1$ if the boolean argument is true and $0$ otherwise * $\mathbf{1}_{\mathcal{X}}(z)$: the set-membership indicator function, evaluates to $1$ if the element $z$ belongs to the set $\mathcal{X}$ and $0$ otherwise * $\mathbf{(\cdot)}^\top$: transpose of a vector or a matrix * $\mathbf{X}^{-1}$: inverse of matrix $\mathbf{X}$ * $\odot$: Hadamard (elementwise) product * $[\cdot, \cdot]$: concatenation * $\|\cdot\|_p$: $L_p$ norm * $\|\cdot\|$: $L_2$ norm * $\langle \mathbf{x}, \mathbf{y} \rangle$: dot product of vectors $\mathbf{x}$ and $\mathbf{y}$ * $\sum$: summation over a collection of elements * $\prod$: product over a collection of elements * $\stackrel{\mathrm{def}}{=}$: an equality asserted as a definition of the symbol on the left-hand side
- `ctx_0f35f327e3ffb1af62c20c1c`: ```{.python .input} #@save class BananasDataset(gluon.data.Dataset): """A customized dataset to load the banana detection dataset.""" def __init__(self, is_train): self.features, self.labels = read_data_bananas(is_train) print('read ' + str(len(self.features)) + (f' training examples' if is_train else f' validation examples')) def __getitem__(self, idx): return (self.features[idx].astype('float32').transpose(2, 0, 1), self.labels[idx]) def __len__(self): return len(self.features) ```
- `ctx_b0979095fea4bcd28cf2ac0f`: The `one_hot` function transforms such a minibatch into a three-dimensional tensor with the last dimension equals to the vocabulary size (`len(vocab)`).**) We often transpose the input so that we will obtain an output of shape (number of time steps, batch size, vocabulary size).
- `ctx_f5d11ac90b12d0ac504bdbf3`: of queries) def forward(self, queries, keys, values, valid_lens=None): d = queries.shape[-1] # Set `transpose_b=True` to swap the last two dimensions of `keys` scores = torch.bmm(queries, keys.transpose(1,2)) / math.sqrt(d) self.attention_weights = masked_softmax(scores, valid_lens) return torch.bmm(self.dropout(self.attention_weights), values) ```

### Backup contexts

- `ctx_282dbeb511791085b7d7db24`: # of tokens, token vector dimension) along vectors embeddings = np.concatenate(( self.embedding(inputs), self.constant_embedding(inputs)), axis=2) # Per the input format of one-dimensional convolutional layers, # rearrange the tensor so that the second dimension stores channels embeddings = embeddings.transpose(0, 2, 1) # For each one-dimensional convolutional layer, after max-over-time # pooling, a tensor of shape (batch size, no.
- `ctx_2514212c3b726616b6545c2c`: ```{.python .input} d2l.set_figsize((4, 4)) for X, y in data_iter: imgs = X[0:20,:,:,:].transpose(0, 2, 3, 1)/2+0.5 d2l.show_images(imgs, num_rows=4, num_cols=5) break ```
- `ctx_d57bee107ebd48e4888a2c07`: ```{.python .input} #@tab all corr2d(d2l.transpose(X), K) ```

### Contrastive contexts

- `ctxx_7b18eca1441d8ef083130efa`: Synthetic: In music, transpose a melody up by two semitones.

### Definition evidence

- `ctx_8b4f9d8a2db0a1027760532e`: When we exchange a matrix's rows and columns, the result is called the *transpose* of the matrix.
- `ctx_b0979095fea4bcd28cf2ac0f`: The `one_hot` function transforms such a minibatch into a three-dimensional tensor with the last dimension equals to the vocabulary size (`len(vocab)`).**) We often transpose the input so that we will obtain an output of shape (number of time steps, batch size, vocabulary size).
- `ctx_f5d11ac90b12d0ac504bdbf3`: of queries) def forward(self, queries, keys, values, valid_lens=None): d = queries.shape[-1] # Set `transpose_b=True` to swap the last two dimensions of `keys` scores = torch.bmm(queries, keys.transpose(1,2)) / math.sqrt(d) self.attention_weights = masked_softmax(scores, valid_lens) return torch.bmm(self.dropout(self.attention_weights), values) ```

### Part-of-speech evidence

- `ctx_b0979095fea4bcd28cf2ac0f`: The `one_hot` function transforms such a minibatch into a three-dimensional tensor with the last dimension equals to the vocabulary size (`len(vocab)`).**) We often transpose the input so that we will obtain an output of shape (number of time steps, batch size, vocabulary size).
- `ctx_f5d11ac90b12d0ac504bdbf3`: of queries) def forward(self, queries, keys, values, valid_lens=None): d = queries.shape[-1] # Set `transpose_b=True` to swap the last two dimensions of `keys` scores = torch.bmm(queries, keys.transpose(1,2)) / math.sqrt(d) self.attention_weights = masked_softmax(scores, valid_lens) return torch.bmm(self.dropout(self.attention_weights), values) ```

## 5. VGG network

- `sense_id`: `d2lce_7be1455c48134d411f51fe7c`
- Split: `test`
- Model definition: a named convolutional neural network architecture with stacked convolutional blocks and fully connected layers
- Model POS: `proper_noun`

### Primary contexts

- `ctx_a33fa21e3115491f18e2796d`: ## [**VGG Network**]
- `ctx_b148bf8074b973465bfd0498`: In order to extract the content features and style features of the image, we can select the output of certain layers in the VGG network.
- `ctx_63a7e0037fac12cc4fdcdd35`: The original VGG network had 5 convolutional blocks, among which the first two have one convolutional layer each and the latter three contain two convolutional layers each.
- `ctx_ece546a2d6d19914a8d8e91c`: Like AlexNet and LeNet, the VGG Network can be partitioned into two parts: the first consisting mostly of convolutional and pooling layers and the second consisting of fully-connected layers.
- `ctx_bcaefb51afe4e2f9d849b9e3`: The fully-connected part of the VGG network is identical to that covered in AlexNet.

### Backup contexts

- `ctx_5de6c0208178676906d2a0d9`: As mentioned in :numref:`sec_vgg`, the VGG network uses 5 convolutional blocks.
- `ctx_d44b321b97faec6a56ab5c33`: For example, the original single-shot multibox detection paper adopts a VGG network truncated before the classification layer :cite:`Liu.Anguelov.Erhan.ea.2016`, while ResNet has also been commonly used.
- `ctx_cb058f206f42d3b06990201c`: These models include AlexNet, the first large-scale network deployed to beat conventional computer vision methods on a large-scale vision challenge; the VGG network, which makes use of a number of repeating blocks of elements; the network in network (NiN) which convolves whole neural networks patch-wise over inputs; GoogLeNet, which uses networks with parallel concatenations; residual networks (ResNet), which remain the most popular off-the-shelf architecture in computer vision; and densely connected networks (DenseNet), which are expensive to compute but have set some recent benchmarks.

### Contrastive contexts

- `ctxx_b0a67e051ec709b51f04bce9`: Synthetic: VGG network sponsored the event as a media company.

### Definition evidence

- `ctx_a33fa21e3115491f18e2796d`: ## [**VGG Network**]
- `ctx_ece546a2d6d19914a8d8e91c`: Like AlexNet and LeNet, the VGG Network can be partitioned into two parts: the first consisting mostly of convolutional and pooling layers and the second consisting of fully-connected layers.
- `ctx_63a7e0037fac12cc4fdcdd35`: The original VGG network had 5 convolutional blocks, among which the first two have one convolutional layer each and the latter three contain two convolutional layers each.

### Part-of-speech evidence

- `ctx_a33fa21e3115491f18e2796d`: ## [**VGG Network**]
- `ctx_ece546a2d6d19914a8d8e91c`: Like AlexNet and LeNet, the VGG Network can be partitioned into two parts: the first consisting mostly of convolutional and pooling layers and the second consisting of fully-connected layers.
