# Stage A sense casebook: validation_001

This casebook contains no Vietnamese candidates. Review the English
sense definition and part of speech from supplied evidence only.

## 1. address

- `sense_id`: `d2lce_89406dadb8c277a1b688698c`
- Split: `validation`
- Model definition: to deal with, discuss, or work on a topic, question, problem, or issue
- Model POS: `verb`

### Primary contexts

- `ctx_6b7bea549646404f1beed42e`: To address classification with linear models, we will need as many affine functions as we have outputs.
- `ctx_6547ed8c4718efbe943c8076`: While some interactive resources have popped up sporadically to address a particular topic, e.g., the engaging blog posts published on the website [Distill](http://distill.pub), or personal blogs, they only covered selected topics in deep learning, and often lacked associated code.
- `ctx_1a28cd982468e0724021f121`: Since sequence data are by their very nature sequential, we need to address the issue of processing it.
- `ctx_58035c4ed6978f0d5bd45d62`: We will address unsupervised learning techniques in later chapters.
- `ctx_b1a8388ce3ee8e26151f7cfb`: One focus of the paper was to address the question of which sized convolution kernels are best.

### Backup contexts

- `ctx_b6b456326d86e2b5df1c872c`: And yet despite the apparent absurdity of linearity here, as compared with our previous examples, it is less obvious that we could address the problem with a simple preprocessing fix.
- `ctx_b618fd3842fb14f923c5d936`: There are still many operations that we need to address.

### Contrastive contexts

- `ctx_9eee0f64aed93d6db84ffda8`: In the following example, we demonstrate this with Python's `id()` function, which gives us the exact address of the referenced object in memory.

### Definition evidence

- `ctx_6547ed8c4718efbe943c8076`: While some interactive resources have popped up sporadically to address a particular topic, e.g., the engaging blog posts published on the website [Distill](http://distill.pub), or personal blogs, they only covered selected topics in deep learning, and often lacked associated code.
- `ctx_58035c4ed6978f0d5bd45d62`: We will address unsupervised learning techniques in later chapters.
- `ctx_6b7bea549646404f1beed42e`: To address classification with linear models, we will need as many affine functions as we have outputs.
- `ctx_b6b456326d86e2b5df1c872c`: And yet despite the apparent absurdity of linearity here, as compared with our previous examples, it is less obvious that we could address the problem with a simple preprocessing fix.
- `ctx_b618fd3842fb14f923c5d936`: There are still many operations that we need to address.
- `ctx_b1a8388ce3ee8e26151f7cfb`: One focus of the paper was to address the question of which sized convolution kernels are best.
- `ctx_1a28cd982468e0724021f121`: Since sequence data are by their very nature sequential, we need to address the issue of processing it.

### Part-of-speech evidence

- `ctx_6547ed8c4718efbe943c8076`: While some interactive resources have popped up sporadically to address a particular topic, e.g., the engaging blog posts published on the website [Distill](http://distill.pub), or personal blogs, they only covered selected topics in deep learning, and often lacked associated code.
- `ctx_58035c4ed6978f0d5bd45d62`: We will address unsupervised learning techniques in later chapters.
- `ctx_6b7bea549646404f1beed42e`: To address classification with linear models, we will need as many affine functions as we have outputs.
- `ctx_b6b456326d86e2b5df1c872c`: And yet despite the apparent absurdity of linearity here, as compared with our previous examples, it is less obvious that we could address the problem with a simple preprocessing fix.
- `ctx_b618fd3842fb14f923c5d936`: There are still many operations that we need to address.
- `ctx_b1a8388ce3ee8e26151f7cfb`: One focus of the paper was to address the question of which sized convolution kernels are best.
- `ctx_1a28cd982468e0724021f121`: Since sequence data are by their very nature sequential, we need to address the issue of processing it.

## 2. attention

- `sense_id`: `d2lce_4a5f20bb32b66b1f07d9f37a`
- Split: `validation`
- Model definition: focused notice or concentration; in this book also the named modeling idea behind attention mechanisms
- Model POS: `noun`

### Primary contexts

- `ctx_05ff6c3d6948601de78e83f7`: * Attention mechanisms solved a second problem that had plagued statistics for over a century: how to increase the memory and complexity of a system without increasing the number of learnable parameters.
- `ctx_31a4a8b87d7c576fc191dfeb`: # Attention Mechanisms :label:`chap_attention`
- `ctx_dcce5005701acc61787285d0`: In :numref:`chap_attention`, we introduce a relatively new class of models based on so-called attention mechanisms that has displaced RNNs as the dominant architecture for most natural language processing tasks.
- `ctx_0226e0426283375d7ada52a5`: To accomplish the latter we need to pay attention to overfitting in addition to using the optimization algorithm to reduce the training error.
- `ctx_9b75794de1b3d229a80c72a0`: Pay special attention to the training and inference cost.

### Contrastive contexts

- `ctxx_38fbfb8ab20bbaae0ce535cb`: Synthetic: Here, attention refers to a focus-weighting idea in models, not to legal or medical emergency attention.

### Definition evidence

- `ctx_31a4a8b87d7c576fc191dfeb`: # Attention Mechanisms :label:`chap_attention`
- `ctx_05ff6c3d6948601de78e83f7`: * Attention mechanisms solved a second problem that had plagued statistics for over a century: how to increase the memory and complexity of a system without increasing the number of learnable parameters.
- `ctx_9b75794de1b3d229a80c72a0`: Pay special attention to the training and inference cost.

### Part-of-speech evidence

- `ctx_31a4a8b87d7c576fc191dfeb`: # Attention Mechanisms :label:`chap_attention`
- `ctx_0226e0426283375d7ada52a5`: To accomplish the latter we need to pay attention to overfitting in addition to using the optimization algorithm to reduce the training error.

## 3. binary classification

- `sense_id`: `d2lce_bb2aa6e567ff5ce9563e209b`
- Split: `validation`
- Model definition: classification with exactly two possible classes or labels.
- Model POS: `noun_phrase`

### Primary contexts

- `ctx_fd7b1833cfe32e71286ed088`: The simplest form of classification is when there are only two classes, a problem which we call *binary classification*.
- `ctx_0c105541407715dba3c8320d`: Sigmoids are still widely used as activation functions on the output units, when we want to interpret the outputs as probabilities for binary classification problems (you can think of the sigmoid as a special case of the softmax).
- `ctx_01284a3b6932dfc4de14bac9`: Depending on what we want to do with our model ultimately, treating this as a binary classification problem might not make a lot of sense.
- `ctx_1f8c290f8b62295034a6b072`: To help understand the relationship between two text sequences, BERT considers a binary classification task, *next sentence prediction*, in its pretraining.
- `ctx_aa5c3843ce520012c58d91a8`: * Click-through rate prediction is usually converted to a binary classification problem.

### Backup contexts

- `ctx_3ac83e193e5c8c27fd8e98a3`: Consider a binary classification problem, where we wish to distinguish between dogs and cats.
- `ctx_d33537b0253739563730301e`: Say we need to solve a binary classification problem based on given $n$ data examples {$x_1, \ldots, x_n$}.
- `ctx_7c5d108c05077e7e7939d072`: In this case, there exists a very effective approach that will give almost as good results as the original: logistic regression, which is a special case of softmax regression (see :numref:`sec_softmax`) for binary classification.

### Contrastive contexts

- `ctxx_30362ebc1c45d47a53816848`: Synthetic: This parser reads a binary classification header from the file format specification.

### Definition evidence

- `ctx_fd7b1833cfe32e71286ed088`: The simplest form of classification is when there are only two classes, a problem which we call *binary classification*.
- `ctx_3ac83e193e5c8c27fd8e98a3`: Consider a binary classification problem, where we wish to distinguish between dogs and cats.

### Part-of-speech evidence

- `ctx_fd7b1833cfe32e71286ed088`: The simplest form of classification is when there are only two classes, a problem which we call *binary classification*.
- `ctx_1f8c290f8b62295034a6b072`: To help understand the relationship between two text sequences, BERT considers a binary classification task, *next sentence prediction*, in its pretraining.

## 4. BPR

- `sense_id`: `d2lce_0dbb5a3cc045f69a43d09a7a`
- Split: `validation`
- Model definition: Bayesian personalized ranking, a pairwise ranking objective or loss for recommender systems
- Model POS: `proper_noun`

### Primary contexts

- `ctx_020ebd08a41a2d0d0dde5114`: Bayesian personalized ranking (BPR) :cite:`Rendle.Freudenthaler.Gantner.ea.2009` is a pairwise personalized ranking loss that is derived from the maximum posterior estimator.
- `ctx_fb0f1c491294165f833df59e`: - Are there any variants of BPR and hinge loss available?
- `ctx_0e9fc9f8cf5ab30d22fb84b2`: The Bayesian formulation of BPR which aims to maximize the posterior probability is given below:
- `ctx_22cd914b3325a8ea0708cb22`: - Can you find any recommendation models that use BPR or hinge loss?
- `ctx_6702f17fcaa78157880ba4f3`: The implementation of BPR loss is as follows.

### Backup contexts

- `ctx_6ea774a351b38a9d8f40a14a`: The training data of BPR consists of both positive and negative pairs (missing values).
- `ctx_9ece3b19ead5f1ddd09582aa`: Similar to BPR, it aims to optimize for relevant distance between positive and negative samples instead of absolute outputs, making it well suited to recommender systems.
- `ctx_db5da8d2cb5b6ed6e11f95de`: $$ \begin{aligned} \text{BPR-OPT} : &= \ln p(\Theta \mid >_u) \\ & \propto \ln p(>_u \mid \Theta) p(\Theta) \\ &= \ln \prod_{(u, i, j \in D)} \sigma(\hat{y}_{ui} - \hat{y}_{uj}) p(\Theta) \\ &= \sum_{(u, i, j \in D)} \ln \sigma(\hat{y}_{ui} - \hat{y}_{uj}) + \ln p(\Theta) \\ &= \sum_{(u, i, j \in D)} \ln \sigma(\hat{y}_{ui} - \hat{y}_{uj}) - \lambda_\Theta \|\Theta \|^2 \end{aligned} $$

### Contrastive contexts

- `ctxx_120dd7c9e9410b193072eb57`: Synthetic: Here BPR names the Bayesian personalized ranking objective, not a generic business process rule.

### Definition evidence

- `ctx_020ebd08a41a2d0d0dde5114`: Bayesian personalized ranking (BPR) :cite:`Rendle.Freudenthaler.Gantner.ea.2009` is a pairwise personalized ranking loss that is derived from the maximum posterior estimator.
- `ctx_0e9fc9f8cf5ab30d22fb84b2`: The Bayesian formulation of BPR which aims to maximize the posterior probability is given below:
- `ctx_6702f17fcaa78157880ba4f3`: The implementation of BPR loss is as follows.

### Part-of-speech evidence

- `ctx_020ebd08a41a2d0d0dde5114`: Bayesian personalized ranking (BPR) :cite:`Rendle.Freudenthaler.Gantner.ea.2009` is a pairwise personalized ranking loss that is derived from the maximum posterior estimator.
- `ctx_6702f17fcaa78157880ba4f3`: The implementation of BPR loss is as follows.

## 5. classification problems

- `sense_id`: `d2lce_7070b47672fcb2311c0527b6`
- Split: `validation`
- Model definition: problems whose goal is to predict which category or label an example belongs to
- Model POS: `noun_phrase`

### Primary contexts

- `ctx_ffdf1a13224ab9169c7fc415`: In subsequent sections, we will introduce classification problems, where the goal is to predict membership among a set of categories.
- `ctx_026fffa1498f9353695d8938`: While we attacked regression problems by trying to minimize the squared error loss function, the common loss function for classification problems is called *cross-entropy*, whose name can be demystified via an introduction to information theory in subsequent chapters.
- `ctx_18f207162816691afd0ac11d`: Auto-tagging problems are typically best described as multi-label classification problems.
- `ctx_275c69012387f631509a7b14`: Some classification problems fit neatly into the binary or multiclass classification setups.
- `ctx_4f3feaf3fe5fc9145ebc21e9`: But general classification problems do not come with natural orderings among the classes.

### Backup contexts

- `ctx_8ee021478cb4ccd5631a27d8`: Sigmoids are still widely used as activation functions on the output units, when we want to interpret the outputs as probabilities for binary classification problems (you can think of the sigmoid as a special case of the softmax).
- `ctx_96aeca71763647a9e736af8b`: * Softmax regression applies to classification problems.
- `ctx_f94fc680303cbcb3c27ab99b`: This loss is called the *cross-entropy loss* and it is one of the most commonly used losses for classification problems.

### Contrastive contexts

- `ctxx_84b6c6da320bc9d34290eccf`: Synthetic: The library faced classification problems when volunteers disagreed on where to shelve rare manuscripts.

### Definition evidence

- `ctx_ffdf1a13224ab9169c7fc415`: In subsequent sections, we will introduce classification problems, where the goal is to predict membership among a set of categories.
- `ctx_026fffa1498f9353695d8938`: While we attacked regression problems by trying to minimize the squared error loss function, the common loss function for classification problems is called *cross-entropy*, whose name can be demystified via an introduction to information theory in subsequent chapters.
- `ctx_4f3feaf3fe5fc9145ebc21e9`: But general classification problems do not come with natural orderings among the classes.

### Part-of-speech evidence

- `ctx_026fffa1498f9353695d8938`: While we attacked regression problems by trying to minimize the squared error loss function, the common loss function for classification problems is called *cross-entropy*, whose name can be demystified via an introduction to information theory in subsequent chapters.
- `ctx_ffdf1a13224ab9169c7fc415`: In subsequent sections, we will introduce classification problems, where the goal is to predict membership among a set of categories.
- `ctx_96aeca71763647a9e736af8b`: * Softmax regression applies to classification problems.

## 6. classification task

- `sense_id`: `d2lce_d89e6e83e5f07d1fcb18c6aa`
- Split: `validation`
- Model definition: a machine learning task in which each example is assigned to one of a set of categories or labels
- Model POS: `noun_phrase`

### Primary contexts

- `ctx_ee34c2df4e3b3b9663241088`: In a classification task, we map an example into a category.
- `ctx_03e87b1a98115f17fc26d0f7`: (Refer to :numref:`sec_softmax` for a more detailed explanation.) One natural way to express the classification task is via the probabilistic question: what is the most likely label given the features (i.e., image pixels)?
- `ctx_c332d46b413066a4b5ce91fc`: Since sentiments can be categorized as discrete polarities or scales (e.g., positive and negative), we can consider sentiment analysis as a text classification task, which transforms a varying-length text sequence into a fixed-length text category.
- `ctx_3e389fe6256706465318f5f8`: To help understand the relationship between two text sequences, BERT considers a binary classification task, *next sentence prediction*, in its pretraining.
- `ctx_5178997f9928de9b50c9dd24`: Assume that we are dealing with a classification task with $k$ categories.

### Backup contexts

- `ctx_7a43f811e64406c483bc4185`: According to descriptions of :numref:`subsec_nsp`, the `_get_next_sentence` function generates a training example for the binary classification task.
- `ctx_d3fc4788f9797ce1f2337e72`: We apply a sigmoid function over the final score since we treat the CTR prediction as a classification task.
- `ctx_85fa1560b684d47c13e4cb02`: They can be used to define decision planes that are often used as the last step in a classification task.

### Contrastive contexts

- `ctxx_a2ae7fe0e53d89ee6a8bb119`: Synthetic: The classification task in the museum was to sort fossils into display trays by species.

### Definition evidence

- `ctx_5178997f9928de9b50c9dd24`: Assume that we are dealing with a classification task with $k$ categories.
- `ctx_03e87b1a98115f17fc26d0f7`: (Refer to :numref:`sec_softmax` for a more detailed explanation.) One natural way to express the classification task is via the probabilistic question: what is the most likely label given the features (i.e., image pixels)?
- `ctx_ee34c2df4e3b3b9663241088`: In a classification task, we map an example into a category.

### Part-of-speech evidence

- `ctx_3e389fe6256706465318f5f8`: To help understand the relationship between two text sequences, BERT considers a binary classification task, *next sentence prediction*, in its pretraining.
- `ctx_c332d46b413066a4b5ce91fc`: Since sentiments can be categorized as discrete polarities or scales (e.g., positive and negative), we can consider sentiment analysis as a text classification task, which transforms a varying-length text sequence into a fixed-length text category.
- `ctx_d3fc4788f9797ce1f2337e72`: We apply a sigmoid function over the final score since we treat the CTR prediction as a classification task.

## 7. convolution kernel

- `sense_id`: `d2lce_7c27fa40b039d07e81508741`
- Split: `validation`
- Model definition: the learnable filter tensor used in a convolution or cross-correlation operation
- Model POS: `noun_phrase`

### Primary contexts

- `ctx_f1e496939bf84efa5b0055a2`: In particular, the difference from :numref:`sec_resnet` is that we use a smaller convolution kernel, stride, and padding at the beginning.
- `ctx_324283e43fc62ecd24e2a923`: In the deep learning research community, $\mathbf{V}$ is referred to as a *convolution kernel*, a *filter*, or simply the layer's *weights* that are often learnable parameters.
- `ctx_3ef46376e083fc08200cbde9`: In the example below, we define a $3\times 3$ input `X` and a $2\times 2$ convolution kernel `K`, and then use the `corr2d` function to compute the convolution output `Y`.
- `ctx_f2e17ee567b215267ffa2690`: Because the kernel has width and height greater than one, we can only properly compute the cross-correlation for locations where the kernel fits wholly within the image, the output size is given by the input size $n_h \times n_w$ minus the size of the convolution kernel $k_h \times k_w$ via
- `ctx_62a689a27a8ee80eaa82689e`: Then for each channel, perform a cross-correlation operation on the one-dimensional tensor of the input and the one-dimensional tensor of the convolution kernel, summing the results over all the channels to produce the one-dimensional output tensor.

### Backup contexts

- `ctx_6bfd2b1f7968c3e4e607ba4a`: Show that in this case the convolution kernel implements an MLP independently for each set of channels.
- `ctx_553b80dd5ccbf185fe342d46`: One focus of the paper was to address the question of which sized convolution kernels are best.
- `ctx_3a70b91cc809a88026050552`: Assume that the size of the convolution kernel is $\Delta = 0$.

### Contrastive contexts

- `ctxx_1e6dd55bc1e9c0b274749deb`: Synthetic: The chef named his new sauce the convolution kernel, but it was only a menu joke.

### Definition evidence

- `ctx_324283e43fc62ecd24e2a923`: In the deep learning research community, $\mathbf{V}$ is referred to as a *convolution kernel*, a *filter*, or simply the layer's *weights* that are often learnable parameters.
- `ctx_3ef46376e083fc08200cbde9`: In the example below, we define a $3\times 3$ input `X` and a $2\times 2$ convolution kernel `K`, and then use the `corr2d` function to compute the convolution output `Y`.
- `ctx_f2e17ee567b215267ffa2690`: Because the kernel has width and height greater than one, we can only properly compute the cross-correlation for locations where the kernel fits wholly within the image, the output size is given by the input size $n_h \times n_w$ minus the size of the convolution kernel $k_h \times k_w$ via

### Part-of-speech evidence

- `ctx_324283e43fc62ecd24e2a923`: In the deep learning research community, $\mathbf{V}$ is referred to as a *convolution kernel*, a *filter*, or simply the layer's *weights* that are often learnable parameters.
- `ctx_553b80dd5ccbf185fe342d46`: One focus of the paper was to address the question of which sized convolution kernels are best.
- `ctx_f1e496939bf84efa5b0055a2`: In particular, the difference from :numref:`sec_resnet` is that we use a smaller convolution kernel, stride, and padding at the beginning.

## 8. dependency

- `sense_id`: `d2lce_144baca5ccb9be2154705e92`
- Split: `validation`
- Model definition: a relation in which one variable, computation, or step relies on another, often across a graph or sequence
- Model POS: `noun`

### Primary contexts

- `ctx_352957138cd800697e9e1625`: Assume that we want to estimate some dependency $P(y \mid \mathbf{x})$ for which we have labeled data $(\mathbf{x}_i, y_i)$.
- `ctx_cd053e813da2a435104cf02a`: However, training LSTMs and other sequence models (such as GRUs) are quite costly due to the long range dependency of the sequence.
- `ctx_0ca5bf2018319128f4a0f958`: Since sequences can be rather long, the dependency can be rather lengthy.
- `ctx_30b0abbdda1b9ee7a038b75e`: The name DenseNet arises from the fact that the dependency graph between variables becomes quite dense.
- `ctx_90f232a47692067501676b09`: In this case the information from $\mathbf{X}_t$ is essentially ignored, effectively skipping time step $t$ in the dependency chain.

### Backup contexts

- `ctx_a78f591991c49716e5760863`: Hence, gradients will have a very long dependency chain.
- `ctx_ebf04bc98574f005873df1b9`: Let us look at another toy example to understand the dependency graph a bit better.
- `ctx_7e604e4269944dde1bd55c75`: Fortunately, the dependency assumption means that the conditional distribution does not change: $p(y \mid \mathbf{x}) = q(y \mid \mathbf{x})$.

### Contrastive contexts

- `ctxx_f023347a554d158072933f48`: Synthetic: The project has a software dependency on a plotting library, which is a package requirement rather than the relation discussed here.

### Definition evidence

- `ctx_352957138cd800697e9e1625`: Assume that we want to estimate some dependency $P(y \mid \mathbf{x})$ for which we have labeled data $(\mathbf{x}_i, y_i)$.
- `ctx_0ca5bf2018319128f4a0f958`: Since sequences can be rather long, the dependency can be rather lengthy.
- `ctx_ebf04bc98574f005873df1b9`: Let us look at another toy example to understand the dependency graph a bit better.

### Part-of-speech evidence

- `ctx_352957138cd800697e9e1625`: Assume that we want to estimate some dependency $P(y \mid \mathbf{x})$ for which we have labeled data $(\mathbf{x}_i, y_i)$.
- `ctx_0ca5bf2018319128f4a0f958`: Since sequences can be rather long, the dependency can be rather lengthy.

## 9. generalization

- `sense_id`: `d2lce_22496a6fabe26e046ad5d5ac`
- Split: `validation`
- Model definition: A model's ability to perform well on unseen data beyond the examples used for training.
- Model POS: `noun_phrase`

### Primary contexts

- `ctx_0d5f7d63c599ed1c30109497`: ## Training Error and Generalization Error
- `ctx_d988f2e78cbcb34054757424`: Thus we can decompose the task of fitting models into two key concerns: (i) *optimization*: the process of fitting our models to observed data; (ii) *generalization*: the mathematical principles and practitioners' wisdom that guide as to how to produce models whose validity extends beyond the exact set of data examples used to train them.
- `ctx_51719f5f7eab3e50160fe2f8`: Even with dropout and weight decay, they remain so flexible that their ability to generalize to unseen data cannot be explained via conventional learning-theoretic generalization guarantees.
- `ctx_d3d2588eb63ac28e8bacb42b`: At the beginning of this chapter, we will describe two methods that may improve model generalization, namely *image augmentation* and *fine-tuning*, and apply them to image classification.
- `ctx_cefb047015d9e81a2c90b5bc`: The more formidable task is to find parameters that will achieve low loss on data that we have not seen before, a challenge called *generalization*.

### Backup contexts

- `ctx_ac57e9818b2139850b558ce7`: However, the goal of deep learning (or more broadly, statistical inference) is to reduce the generalization error.

### Contrastive contexts

- `ctxx_b860c0289e624b4ac611322e`: A hyperplane is a generalization to higher dimensions of a line or a plane.

### Definition evidence

- `ctx_d988f2e78cbcb34054757424`: Thus we can decompose the task of fitting models into two key concerns: (i) *optimization*: the process of fitting our models to observed data; (ii) *generalization*: the mathematical principles and practitioners' wisdom that guide as to how to produce models whose validity extends beyond the exact set of data examples used to train them.
- `ctx_cefb047015d9e81a2c90b5bc`: The more formidable task is to find parameters that will achieve low loss on data that we have not seen before, a challenge called *generalization*.
- `ctx_51719f5f7eab3e50160fe2f8`: Even with dropout and weight decay, they remain so flexible that their ability to generalize to unseen data cannot be explained via conventional learning-theoretic generalization guarantees.
- `ctx_d3d2588eb63ac28e8bacb42b`: At the beginning of this chapter, we will describe two methods that may improve model generalization, namely *image augmentation* and *fine-tuning*, and apply them to image classification.

### Part-of-speech evidence

- `ctx_d988f2e78cbcb34054757424`: Thus we can decompose the task of fitting models into two key concerns: (i) *optimization*: the process of fitting our models to observed data; (ii) *generalization*: the mathematical principles and practitioners' wisdom that guide as to how to produce models whose validity extends beyond the exact set of data examples used to train them.
- `ctx_cefb047015d9e81a2c90b5bc`: The more formidable task is to find parameters that will achieve low loss on data that we have not seen before, a challenge called *generalization*.
- `ctx_d3d2588eb63ac28e8bacb42b`: At the beginning of this chapter, we will describe two methods that may improve model generalization, namely *image augmentation* and *fine-tuning*, and apply them to image classification.

## 10. generalization error

- `sense_id`: `d2lce_9199c3ba27076b186a42df23`
- Split: `validation`
- Model definition: The expected error of a model on new data drawn from the same underlying distribution as the original sample.
- Model POS: `noun_phrase`

### Primary contexts

- `ctx_dc0382ebdca048c1b0714c3e`: ## Training Error and Generalization Error
- `ctx_b89311738d29fd53e93c2b6b`: In order to discuss this phenomenon more formally, we need to differentiate between training error and generalization error.
- `ctx_584bed2e44864afc9655baf9`: The *training error* is the error of our model as calculated on the training dataset, while *generalization error* is the expectation of our model's error were we to apply it to an infinite stream of additional data examples drawn from the same underlying data distribution as our original sample.
- `ctx_7edf6686ee0adbdd3a986dfc`: In practice, we must *estimate* the generalization error by applying our model to an independent test set constituted of a random selection of data examples that were withheld from our training set.
- `ctx_bfb4aa499a19b3c234af5b7e`: Problematically, we can never calculate the generalization error exactly.

### Backup contexts

- `ctx_54088257ccb95a4974f91f90`: As we increase the amount of data, the probability that the fraction of heads will deviate significantly from $\frac{1}{2}$ diminishes, and our training error would come to match the generalization error.
- `ctx_0737ea14d11a1ebada12a0eb`: However, the goal of deep learning (or more broadly, statistical inference) is to reduce the generalization error.
- `ctx_8cd3de6c9b491391cc9a6104`: In this case, the model that always predicts class 1 will incur an error of $\frac{1}{3}$, considerably better than our generalization error.

### Contrastive contexts

- `ctxx_419574fe3c1e66af04e29fe0`: Synthetic: The child's generalization error in storytelling was not measured statistically.

### Definition evidence

- `ctx_584bed2e44864afc9655baf9`: The *training error* is the error of our model as calculated on the training dataset, while *generalization error* is the expectation of our model's error were we to apply it to an infinite stream of additional data examples drawn from the same underlying data distribution as our original sample.
- `ctx_7edf6686ee0adbdd3a986dfc`: In practice, we must *estimate* the generalization error by applying our model to an independent test set constituted of a random selection of data examples that were withheld from our training set.
- `ctx_bfb4aa499a19b3c234af5b7e`: Problematically, we can never calculate the generalization error exactly.

### Part-of-speech evidence

- `ctx_dc0382ebdca048c1b0714c3e`: ## Training Error and Generalization Error
- `ctx_b89311738d29fd53e93c2b6b`: In order to discuss this phenomenon more formally, we need to differentiate between training error and generalization error.
- `ctx_0737ea14d11a1ebada12a0eb`: However, the goal of deep learning (or more broadly, statistical inference) is to reduce the generalization error.
