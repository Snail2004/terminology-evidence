# MAIN — ĐIỀU PHỐI ONE-SESSION C + E

## Freeze before coding

Publish exact bytes/hashes for:

```text
C base commit/tree
E base commit/tree
underflow candidate set
FrozenContextRoleMapV1
EmbeddingModelAuthorityV1
candidate query plans
approved variants
execution boundary
```

## Parallel work

```text
Track C:
role-map + critical arbitration

Track E:
BGE-M3 candidate-specific retrieval
```

Do not ask either track to redesign architecture.

## Integration order

```text
C focused/full PASS
+
E focused/full PASS
        ↓
underflow 3-candidate regression
        ↓
Global replay
        ↓
combined independent-review package
```

## Main must not authorize in this task

```text
gold opening
full D0 remainder
production auto-approval
unbounded acquisition
embedding-based attestation decision
```

## Review package

Include three independent targets:

```text
C narrow child
E narrow child
underflow regression
```

Add complete Git bundles, patches, source ZIPs, JUnit, A/B deterministic build,
CHECKSUMS and credential scan.
